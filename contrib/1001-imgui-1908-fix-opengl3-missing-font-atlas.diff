Recreate the font atlas in the ImGUI OpenGL3 backend if necessary.
Fixes TFE's accessbility display when dynamically changing fonts.

--- a/imgui_impl_opengl3.cpp
+++ b/imgui_impl_opengl3.cpp
@@ -402,6 +402,8 @@ void    ImGui_ImplOpenGL3_NewFrame()
 
     if (!bd->ShaderHandle)
         ImGui_ImplOpenGL3_CreateDeviceObjects();
+    if (!bd->FontTexture)
+        ImGui_ImplOpenGL3_CreateFontsTexture();
 }
 
 static void ImGui_ImplOpenGL3_SetupRenderState(ImDrawData* draw_data, int fb_width, int fb_height, GLuint vertex_array_object)
-- 
2.45.2
